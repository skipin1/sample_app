<% if @user.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Форма содержит <%= pluralize(@user.errors.count, "ошибка") %>.
    </div>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>